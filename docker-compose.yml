services:
  notebooklm-limit-splitter:
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    image: notebooklm-limit-splitter:local
    user: "node"
    environment:
      PORT: "80"
      STATS_DATA_DIR: /data
    expose:
      - "80"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1/health"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
    volumes:
      - stats-data:/data
    networks:
      - app
    restart: unless-stopped

networks:
  app:
    name: notebooklm-limit-splitter_app
    internal: true

volumes:
  stats-data:
